---
import Layout from "../../layouts/Layout.astro";
/* import { getTrips } from "../viajes/trip.api"; */

/* const trips = await getTrips(); */
---

<Layout
  title="Viajes | Pablo Dive"
  description="Viajes y expediciones relacionados con el submarinismo"
>
  <div class="bg-slate-50 min-h-screen">
    <!-- Header -->
    <div class="bg-white py-20 px-6 text-center border-b border-slate-200">
      <h1 class="text-4xl md:text-6xl font-bold text-blue-800 mb-4">
        Próximas Expediciones
      </h1>
      <p class="text-slate-700 text-base md:text-lg max-w-2xl mx-auto">
        Descubre la naturaleza conmigo y únete para apoyar la conservación.
      </p>
    </div>

    <!-- Grid de viajes comentado temporalmente para mostrar slides de "Próximamente" -->
    {/**
    <div class="max-w-7xl mx-auto px-6 py-20">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          trips.map((trip) => (
            <a
              href={`/trips/${trip.slug}`}
              class="group bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1 flex flex-col h-full"
            >
              <div class="relative aspect-[4/3] overflow-hidden">
                <img
                  src={trip.image.url}
                  alt={trip.title}
                  class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                />
                <div class="absolute top-4 right-4 bg-white/90 backdrop-blur-sm px-3 py-1 rounded-full text-xs font-bold text-blue-800 uppercase tracking-wider">
                  {trip.duration} Días
                </div>
              </div>
              <div class="p-6 flex flex-col flex-1">
                <div class="flex items-center gap-2 text-xs font-bold text-cyan-600 uppercase tracking-widest mb-3">
                  <span>📍 {trip.destination}</span>
                </div>
                <h2 class="text-2xl md:text-4xl font-bold text-slate-900 mb-3 group-hover:text-cyan-600 transition-colors">
                  {trip.title}
                </h2>
                <p class="text-slate-500 text-sm md:text-base line-clamp-2 mb-6">
                  {trip.summary}
                </p>
                <div class="flex justify-between items-center pt-6 border-t border-slate-100 mt-auto">
                  <span class="text-slate-400 text-sm">Desde</span>
                  <span class="text-xl md:text-2xl font-bold text-slate-900">
                    {trip.price}€
                  </span>
                </div>
              </div>
            </a>
          ))
        }
      </div>
    </div>
    */}

    <!-- Slides "Próximamente" -->
    <div class="max-w-7xl mx-auto px-6 py-20">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {[1,2,3].map(() => (
          <div class="group bg-white rounded-2xl overflow-hidden shadow-sm flex flex-col h-full items-center justify-center py-12">
            <div class="flex flex-col items-center">
              <div class="w-32 h-24 flex items-center justify-center mb-6">
                {/* SVG de interrogación */}
                <svg width="72" height="72" viewBox="0 0 72 72" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <circle cx="36" cy="36" r="32" fill="#e0e7ef" fill-opacity="0.7" stroke="#1e293b" stroke-width="2"/>
                  <path d="M36 48v-2m0-6c0-4 6-4 6-10a6 6 0 10-12 0" stroke="#2563eb" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
                  <circle cx="36" cy="56" r="2.5" fill="#2563eb"/>
                </svg>
              </div>
              <h2 class="text-2xl md:text-4xl font-bold text-slate-900 mb-2 text-center">Próximamente</h2>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</Layout>
