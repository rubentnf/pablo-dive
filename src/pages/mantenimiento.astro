---
---


<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Sitio en Mantenimiento - Pablo Dive</title>
    <meta name="robots" content="noindex, nofollow" />
    <style>
      body {
        min-height: 100vh;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: #fff;
        font-family: 'Inter', Arial, sans-serif;
        text-align: center;
        padding: 0 1rem;
      }
      .container {
        max-width: 600px;
        width: 100%;
        background: rgba(255,255,255,0.08);
        border-radius: 15px;
        padding: 2.5rem 1.5rem;
        box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        position: relative;
      }
      .icon {
        font-size: 5rem;
        margin-bottom: 2rem;
        animation: bounce 1.5s infinite;
      }
      @keyframes bounce {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-20px); }
      }
      h1 {
        font-size: clamp(2rem,5vw,3.5rem);
        font-weight: bold;
        margin-bottom: 1.5rem;
        line-height: 1.2;
      }
      p {
        font-size: 1.1rem;
        opacity: 0.95;
        margin-bottom: 1.5rem;
        line-height: 1.6;
      }
      .contact {
        margin-top: 2rem;
        background: rgba(255,255,255,0.10);
        border-radius: 15px;
        padding: 2rem;
      }
      .contact a {
        display: inline-flex;
        align-items: center;
        gap: 0.8rem;
        background: rgba(255,255,255,0.20);
        color: #fff;
        padding: 0.8rem 1.5rem;
        border-radius: 10px;
        font-size: 1.1rem;
        text-decoration: none;
        transition: background 0.3s, transform 0.3s;
      }
      .contact a:hover {
        background: rgba(255,255,255,0.30);
        transform: translateY(-3px);
      }
      .contact svg {
        width: 1.5em;
        height: 1.5em;
        fill: currentColor;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="icon">🔧</div>
      <h1>Estamos mejorando tu experiencia</h1>
      <p>Nuestro sitio está en mantenimiento mientras añadimos nuevas funcionalidades increíbles.</p>
      <p style="opacity:0.85;">Volveremos muy pronto con nuevas aventuras submarinas.</p>
    </div>
  </body>
